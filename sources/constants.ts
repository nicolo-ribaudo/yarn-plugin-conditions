// We must update this every time that the generated proxy dependency changes.
export const CACHE_VERSION = 1.8;

// We always set the same mtime in generated zip archives, to keep the checksum static.
export const mtime = 1580511600000;
